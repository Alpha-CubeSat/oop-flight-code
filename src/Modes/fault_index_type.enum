#ifndef fault_index_type_enum_
#define fault_index_type_enum_

enum class fault_index_type : unsigned char
{
    // FAULT1
    mag_x,
    mag_y,
    mag_z,
    gyro_x,
    gyro_y,
    gyro_z,
    acc_x,
    acc_y,

    // FAULT2
    temp_c,
    solar_current,
    voltage,

    // FAULT3
    burn_wire,
    sd_card,
    camera_on_failed,
    light_val,
    button,

    no_fault

};

#endif